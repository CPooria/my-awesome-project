name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # یا هر شاخه‌ای که شاخه اصلی شماست

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18' # نسخه نود مورد نظر خود را وارد کنید

      - name: Install Dependencies 📦
        run: npm install

      - name: Build 🏗️
        run: npm run build

      - name: Deploy 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build # نام پوشه خروجی build را اینجا وارد کنید (مثلاً ./dist)

